<% if user.errors.any? %>
    <div class="alert alert-danger">
        <div><%= pluralize(user.errors.count, "error") %> prohibited this action:
        </div>    
        <ul>
        <% user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
        </ul>
    </div>
<% end %>